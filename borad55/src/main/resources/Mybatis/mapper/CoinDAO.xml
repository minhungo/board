<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.minhun.dao.CoinDAO">


<insert id="ChargeCoin" parameterType="com.demo.minhun.dto.CoinDTO">
    insert into
        coin (payAmount, payChargeDate, signupId)
    values
        (#{payAmount}, current_date, #{signupId})
</insert>

<select id="getMyCoinRecord" resultType="com.demo.minhun.dto.RefundDTO">
    select
        s.signup_id, s.signup_coin, c.payamount, c.paychargedate
    from
        signup s inner join coin c
    on
        s.signup_id = c.signupid
    where
        s.signup_id = #{signup_id}
</select>


</mapper>

