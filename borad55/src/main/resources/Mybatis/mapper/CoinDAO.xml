<?xml version="1.0" encoding="UTF-8"?>


<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.demo.minhun.dao.CoinDAO">


<insert id="ChargeCoin">
    insert into
        coin (payAmount, payChargeDate, signupId)
    values
        (#{param1}, current_date, #{param2})
</insert>

<select id="getMyCoinRecord" resultType="com.demo.minhun.dto.CoinDTO">
    select sum(payAmount) from Coin where signupId in (#{param1})
       
</select>


</mapper>

